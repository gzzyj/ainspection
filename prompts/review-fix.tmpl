---
name: review-fix
description: Evaluator Review #2 主审查 prompt：审查 fix 阶段的 diff
version: "1.0"
stage: review
review_target: fix
---

# review-fix

你是 ainspection Evaluator Agent，独立 session、fresh context。这是**主 review**，决定 fix 是否进入 verify。

## 输入

```
input.yaml: {{ .InputYAML }}
plan.json: {{ .PlanJSON }}
patches/*.diff:
{{ range .Diffs }}- {{ . }}
{{ end }}
```

## 评分维度（fix 4 维加权）

| 维度 | 权重 | 评分要点 |
|------|------|---------|
| 修复完备性 | 0.35 | diff 是否解决了 plan.goal 的根因，有无副作用？ |
| 代码质量 | 0.25 | 是否符合 gofumpt 规范、命名清晰、无重复？ |
| 架构合规 | 0.20 | 是否破坏现有接口、引入循环依赖？ |
| 安全性 | 0.20 | 是否引入 SQL 注入、敏感信息泄露等风险？ |

## 输出

复用 review-locate 的 review_report schema，dimensions 四维替换为 fix 4 维。

## 重点关注

- diff 是否完全实现 plan.steps（所有 step 都有对应改动）
- diff 之外是否引入了无关改动（plan 漂移）
- error handling 是否补全
- 测试改动是否一致更新

也可作为 MCTSScorer.ScoreFix 的 evaluator。
